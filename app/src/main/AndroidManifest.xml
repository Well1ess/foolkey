<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a29149.yuyuan">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- 小米推送所需权限 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <permission
        android:name="com.example.a29149.yuyuan.permission.MIPUSH_RECEIVE"
        android:protectionLevel="signature" />
    <!-- 这里com.xiaomi.mipushdemo改成app的包名 -->
    <uses-permission android:name="com.example.a29149.yuyuan.permission.MIPUSH_RECEIVE" />

    <!-- 这里com.xiaomi.mipushdemo改成app的包名 -->
    <application
        android:name=".Application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppNoBar">
        <activity android:name=".Main.MainActivity"
            android:launchMode="singleTask"></activity>

        <activity android:name=".Login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login.RegisterActivity" />
        <activity android:name=".Login.ModifyPwdActivity" />
        <activity
            android:name=".Search.SearchActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".Model.Index.Course.CourseActivity" />
        <activity android:name=".OriginIndex.OriginIndexActivity" />
        <activity android:name=".Model.Discovery.Activity.RewardActivity" />
        <activity android:name=".Model.Discovery.Activity.QAActivity" />
        <activity android:name=".Model.Discovery.Activity.ArticleActivity" />
        <activity android:name=".Model.Me.Setting.SettingActivity" />
        <activity android:name=".Model.Index.Course.BuyCourseActivity" />
        <activity android:name=".Model.Index.Course.NewCourseOrderActivity" />
        <activity android:name=".Model.Me.Coupon.CouponActivity" />
        <activity android:name=".Model.Me.Recharge.RechargeActivity" />

        <!-- 学生发布悬赏信息描述，已废弃 -->
        <activity android:name=".Model.Publish.Activity.PublishRewardStudentActivity" />
        <!-- 学生发布问题信息描述 -->
        <activity android:name=".Model.Publish.Activity.PublishQuestionStudentActivity" />
        <!-- 老师发布文章描述 -->
        <activity android:name=".Model.Publish.Activity.PublishArticalTeacherActivity" />
        <!-- 老师发布课程信息描述,已废弃 -->
        <activity android:name=".Model.Publish.Activity.PublishCourseTeacherActivity" />
        <!-- 学生发布悬赏信息描述 -->
        <activity android:name=".Model.Publish.Activity.PublishRewardDescribeStudentActivity" />
        <!-- 学生发布悬赏价格信息描述 -->
        <activity android:name=".Model.Publish.Activity.PublishRewardPriceStudentActivity" />
        <!-- 学生发布悬赏选项信息描述 -->
        <activity android:name=".Model.Publish.Activity.PublishRewardOptionsStudentActivity" />

        <!-- 小米推送所需服务 -->
        <!-- 老师发布课程信息描述 -->
        <activity android:name=".Model.Publish.Activity.PublishCoursedDescribeTeacherActivity" />
        <!-- 老师发布课程价格描述 -->
        <activity android:name=".Model.Publish.Activity.PublishCoursePriceTeacherActivity" />
        <!-- 老师发布课程选项信息描述 -->
        <activity android:name=".Model.Publish.Activity.PublishCourseOptionsTeacherActivity" />

        <!-- 申请认证老师 -->
        <activity android:name=".Model.Publish.Activity.ApplyAuthenticationTeacherActivity" />

        <!-- 老师接单悬赏 -->
        <activity android:name=".Model.Discovery.Activity.ApplyRewardTeacherActivity" />
        <!-- 接单悬赏的老师主页 -->
        <activity android:name=".Model.Me.Reward.TeacherIndexActivity" />
        <!-- 老师界面的主页 -->
        <!-- 评价悬赏订单 -->
        <activity android:name=".Model.Order.activity.CommentRewardActivity" />
        <!-- 评价课程订单 -->
        <activity android:name=".Model.Order.activity.CommentCourseActivity" />
        <!-- 老师拥有的悬赏订单 -->
        <activity android:name=".Teacher.Index.reward.OwnerRewardTeacherActivity" />
        <!-- 老师拥有的课程订单 -->
        <activity android:name=".Teacher.Index.course.OwnerCourseTeacherActivity"/>
        <!-- 切换成老师用户后的主界面 -->
        <activity android:name=".TeacherMain.MainTeacherActivity"/>

        <!-- 小米推送所需服务 -->
        <service
            android:name="com.xiaomi.push.service.XMPushService"
            android:enabled="true"
            android:process=":pushservice" />
        <service
            android:name="com.xiaomi.push.service.XMJobService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:process=":pushservice" />
        <!-- 注：此service必须在3.0.1版本以后（包括3.0.1版本）加入 -->
        <service
            android:name="com.xiaomi.mipush.sdk.PushMessageHandler"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name="com.xiaomi.mipush.sdk.MessageHandleService"
            android:enabled="true" />
        <!-- 注：此service必须在2.2.5版本以后（包括2.2.5版本）加入 -->
        <receiver
            android:name="com.xiaomi.push.service.receivers.NetworkStatusReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.xiaomi.push.service.receivers.PingReceiver"
            android:exported="false"
            android:process=":pushservice">
            <intent-filter>
                <action android:name="com.xiaomi.push.PING_TIMER" />
            </intent-filter>
        </receiver>

        <!-- 配置自定义的BroadcastReceiver -->
        <receiver
            android:name=".BroadcastReceiver.DemoMessageReceiver"
            android:exported="true">

            <!-- 这里com.xiaomi.mipushdemo.DemoMessageRreceiver改成app中定义的完整类名 -->
            <intent-filter>
                <action android:name="com.xiaomi.mipush.RECEIVE_MESSAGE" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.xiaomi.mipush.MESSAGE_ARRIVED" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.xiaomi.mipush.ERROR" />
            </intent-filter>
        </receiver>

        <activity android:name=".Model.Me.Reward.OwnerRewardActivity"></activity>
        <activity android:name=".TeacherInfo.TeacherInfoActivity">
        </activity>
        <activity android:name=".AccessOrder.AccessOrderActivity"
            android:windowSoftInputMode="adjustPan">
        </activity>
        <activity android:name=".TeacherMain.Score.MoreStudent.MoreStudentActivity">
        </activity>
    </application>

</manifest>